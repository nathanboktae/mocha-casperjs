this.casper = require('./setup')(require('casper'));

mocha.setup({
  ui: 'bdd',
  reporter: 'spec'
});

describe('autogenerated', function() {
  before(%s);
  it('test 1', %s);
  after(%s);
});

var runner = mocha.run(function() {
  casper.exit(typeof (runner.stats && runner.stats.failures) === 'number' ? runner.stats.failures : -1);
});
